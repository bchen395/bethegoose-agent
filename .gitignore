# Secrets and local data — never commit
.env
.env*.local
data/

# Local-only agent/editor config
.claude/settings.local.json

# One-off operator scripts (read .env.local, echo live tokens) — keep untracked
scripts/seed-instagram-token.mjs

# Node / Next.js
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.vercel
